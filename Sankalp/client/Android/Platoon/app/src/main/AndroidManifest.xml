<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.latencot.platoon">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".ui.profile.manageprojects.UpdateProjectLocation"
            android:label="@string/title_activity_update_project_location"></activity>

        <activity android:name=".ui.profile.manageprojects.ProjectDetails" />
        <activity android:name=".ui.profile.manageprojects.AddProject" />
        <activity android:name=".ui.profile.manageboats.BoatDetails" />
        <activity android:name=".ui.basicpanel.About" />
        <activity android:name=".ui.profile.manageboats.AddBoat" />
        <activity android:name=".ui.profile.manageboats.CompanyBoatsList" />
        <activity
            android:name=".ui.homefragments.MapsNavActivity"
            android:label="@string/title_activity_maps_nav" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".ui.authentication.LoginWithoutPassword" />
        <activity android:name=".ui.userpanel.ProblemSolutions" />
        <activity android:name=".ui.userpanel.SubmitProblem" />
        <activity android:name=".ui.basicpanel.FeedbackActivity" />
        <activity android:name=".ui.profile.manageprojects.ProjectList" />
        <activity
            android:name=".ui.authentication.UploadFile"
            android:usesCleartextTraffic="true" />
        <activity
            android:name=".ui.authentication.MapPointCenterLocation"
            android:label="@string/title_activity_map_point_center_location" />
        <activity android:name=".ui.authentication.RegisterUser" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".ui.authentication.RegisterCredentials" />
        <activity android:name=".ui.authentication.LoginActivity" />
        <activity
            android:name=".ui.HomeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>